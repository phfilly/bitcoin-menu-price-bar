{
  "name": "bitcoin-menu-price-barr",
  "version": "1.0.0",
  "description": "Get the latest bitcoin price from Luno in your menu bar",
  "dependencies": {
    "axios": "^0.17.1",
    "luno-api": "^1.1.0",
    "menubar": "*"
  },
  "devDependencies": {
    "electron-packager": "^5.1.0",
    "electron-prebuilt": "^0.36.0"
  },
  "main": "main.js",
  "scripts": {
    "build": "electron-packager . Example --platform=darwin --arch=x64 --version=0.36.0 --icon=Icon.icns",
    "start": "electron ."
  }
}
